import React from 'react';
import Link from 'next/link';

export default function page() {
  console.log("RENDERING MELODIES INDEX PAGE");
  
  // Create an array of levels
  const levels = [1, 2, 3, 4, 5, 6];

  return (
    <div className="min-h-screen bg-gray-50 p-8">
      <div className="max-w-lg mx-auto bg-white rounded-lg shadow p-6">
        <h1 className="text-3xl font-bold mb-4">Melody Levels</h1>
        <p className="mb-8">Select a level to continue:</p>
        
        <div className="space-y-4">
          {levels.map(level => (
            <Link key={level} href={`/test-3/${level}`}>
              <div className="block w-full bg-blue-100 p-4 rounded hover:bg-blue-200">
                Level {level}
              </div>
            </Link>
          ))}
        </div>
      </div>
    </div>
  );
}